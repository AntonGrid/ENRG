# ENRG — pitch video (Colosseum submission)

`ENRG_pitch.mp4` — the **project pitch**: who I am, the problem, the solution,
what is already built, why me, and the ask. 2:24, 1920×1080, MP4 (H.264 + AAC).

This is **not** the technical demo. The devnet walkthrough is a separate video
and must stay in the Colosseum *Technical demo* field; this file goes into
*Project pitch*.

## What the video says

| t | section | on screen |
|---|---|---|
| 0:00–0:15 | Who I am — Anton, founder, Vladimir (Russia), rotational shifts at an oil field | kicker + name card |
| 0:15–0:35 | The problem — physical data is not provable: editable exports, double counting, audits, greenwashing | mock utility export with the edited cell struck through, `GREENWASHING` stamp |
| 0:35–1:07 | The solution — Axis Protocol: identity · proof · attestation · policy; SE050 signs on the device; ≥2 staked oracles; contradiction = slashing; mint only after finalisation | pipeline `SE050 DEVICE → PROOF → ORACLE QUORUM → ATTESTATION → MINT` lights up stage by stage |
| 1:07–1:34 | What is done — 55 on-chain instructions, 266 tests, live devnet, 2 oracles, finalised attestation, mint through the gate | big counters + the real mint signature and slot |
| 1:34–1:59 | Why me — self-taught, solo, four months, a mission rather than a sprint | timeline `day 0 → month 4 · today` |
| 1:59–2:20 | The ask — one real pilot (a site with a meter) and mentors who have shipped infra to mainnet | contact block |
| 2:20–2:23 | End card — logo, tagline, repository | — |

The exact rendered timings and every spoken line are in [`narration.md`](narration.md);
`ENRG_pitch.srt` holds the same text as captions.

## Brief compliance

| requirement | how it is met |
|---|---|
| 2:00–2:30 | **2:23.6** — 143.6 s of timeline, 143.63 s in the file (AAC tail) |
| 1920×1080, MP4 | H.264 (CRF 18, 30 fps) + AAC 192 kb/s, `+faststart` |
| dark `#0B1020`, accent `#4FC3F7` | palette in `beats.json → meta`; every frame uses it |
| ENRG logo in a corner | flat accent mark + wordmark, top left of every scene |
| on-screen text synced to the narration | the text of a beat appears exactly when its spoken line starts — timings are **measured** from the synthesized audio, not guessed |
| smooth scene transitions | each scene fades in/out (0.45 s), so cuts pass through the dark background |
| calm male English voice | piper `en_US-ryan-high`, `length_scale 1.18` |
| quiet bed | a low ambient pad at −16 LUFS integrated, −1.5 dBTP ceiling |

## Files

| file | role |
|---|---|
| `beats.json` | the script: scenes, spoken lines, on-screen text, palette, timings — and `meta.endcard_stats`, the single source for the closing numbers |
| `render.py` | the pipeline: TTS → timings → ImageMagick backgrounds → ffmpeg scenes → concat → audio mix |
| `extras.py` | regenerates `narration.md` and `ENRG_pitch.srt` from the cached audio |
| `build.sh` | one-shot build (fetches piper + the voice, prepares the fonts) |
| `ENRG_pitch.mp4` | the deliverable |
| `narration.md`, `ENRG_pitch.srt` | script with timings, captions |

## Rebuilding

```bash
bash demo/pitch-video/build.sh            # everything
python3 demo/pitch-video/render.py s3     # one scene (iteration)
python3 demo/pitch-video/render.py --assemble-only
python3 demo/pitch-video/extras.py        # narration.md + captions
```

Needs `ffmpeg`, ImageMagick (`convert`), `python3`, `curl`; no root. On the
first run it downloads the piper binary and the `en_US-ryan-high` voice model
(~120 MB) into `/tmp/assets` and builds the TTFs from the brand faces
(`@fontsource/space-grotesk`, `@fontsource/jetbrains-mono`), so the video uses
exactly the brand fonts. Those faces are read from the `landing` submodule when
its `node_modules` is installed and otherwise fetched from the npm CDN mirror
(`PITCH_FONTS_SRC`, `PITCH_FONTS_CDN`, `PITCH_FONTS_VER` override where they come
from) — three woff2 files should not require an `npm ci` of the whole site.

Change the script by editing `beats.json` and re-running; nothing else hard-codes
the words, and re-running is deterministic for a given voice model.

## Publishing

1. Upload `ENRG_pitch.mp4` to YouTube (unlisted is fine) and load
   `ENRG_pitch.srt` as the caption track.
2. In the Colosseum project card put that link into **Project pitch** and keep the
   existing devnet walkthrough in **Technical demo** — do not reuse one link for
   both fields.
3. Keep `docs/COLOSSEUM-SUBMISSION.md` §2–§4 in sync with whatever the card says.

## Credits and licensing

Voice: [piper](https://github.com/rhasspy/piper) (MIT) with the
`en_US-ryan-high` voice. Fonts: Space Grotesk and JetBrains Mono (SIL OFL 1.1,
taken from the landing build). Everything else is generated by the scripts in
this directory.
