# ==========================================================
# ENRG Protocol
# Git Ignore
# ==========================================================

# ----------------------------------------------------------
# Rust / Anchor
# ----------------------------------------------------------
target/

# ----------------------------------------------------------
# Node.js
# ----------------------------------------------------------
node_modules/
oracle/node_modules/

# ----------------------------------------------------------
# Python
# ----------------------------------------------------------
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ----------------------------------------------------------
# Environment & Secrets
# ----------------------------------------------------------
.env
.env.*
founder-keypair.json
*.key
*.pem

# ----------------------------------------------------------
# Databases
# ----------------------------------------------------------
enrg.db
*.sqlite
*.sqlite3

# ----------------------------------------------------------
# Logs
# ----------------------------------------------------------
*.log

# ----------------------------------------------------------
# Temporary files
# ----------------------------------------------------------
*.tmp
*.bak
*.save
*.swp
.aider*

# ----------------------------------------------------------
# IDE
# ----------------------------------------------------------
.vscode/
.idea/

# ----------------------------------------------------------
# Operating Systems
# ----------------------------------------------------------
.DS_Store
Thumbs.db
test-ledger/

# Secret keys
*-keypair.json
admin-keypair.json
oracle-keypair.json
# P0-1 (audit 2026-08-30): the founder private key was committed at d3664c1.
# Removed from the index; never re-add ANY founder wallet file. Mainnet MUST
# use a rotated key (see MAINNET-CHECKLIST.md).
founder-wallet.json
founder-wallet*.json
# The localnet genesis fixture is NOT a secret (public key + lamports only) —
# it must stay tracked for the deterministic anchor test bootstrap.
!tests/genesis/founder-wallet.json
# Pre-existing: other operator wallets must not be tracked either.
enrg-wallet.json
validator.log
test-ledger/
solana-release/
solana-release-x86_64-unknown-linux-gnu.tar.bz2
keypairs/
enrg-wallet.json
.config/
deploy/
# OTA-образы прошивки, публикуемые оракулом (ADR-0008)
firmware/updates/
# Legacy-прошивки v1/v2 (заархивированы, НЕ трекать — D-1, ADR-0001/0007)
firmware/legacy/
