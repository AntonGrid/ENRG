# ==========================================================
# ENRG Protocol
# Git Ignore
# ==========================================================

# ----------------------------------------------------------
# Rust / Anchor
# ----------------------------------------------------------
target/

# ----------------------------------------------------------
# Node.js
# ----------------------------------------------------------
node_modules/
oracle/node_modules/

# ----------------------------------------------------------
# Python
# ----------------------------------------------------------
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ----------------------------------------------------------
# Environment & Secrets
# ----------------------------------------------------------
.env
.env.*
# ...except the documented example, which is meant to be committed:
!.env.example
founder-keypair.json
*.key
*.pem

# ----------------------------------------------------------
# Databases
# ----------------------------------------------------------
enrg.db
*.sqlite
*.sqlite3

# ----------------------------------------------------------
# Logs
# ----------------------------------------------------------
*.log

# ----------------------------------------------------------
# Temporary files
# ----------------------------------------------------------
*.tmp
*.bak
*.save
*.swp
.aider*

# ----------------------------------------------------------
# IDE
# ----------------------------------------------------------
.vscode/
.idea/

# ----------------------------------------------------------
# Operating Systems
# ----------------------------------------------------------
.DS_Store
Thumbs.db
test-ledger/

# Secret keys
*-keypair.json
admin-keypair.json
oracle-keypair.json
# P0-1 (audit 2026-08-30): the founder private key was committed at d3664c1.
# Removed from the index; never re-add ANY founder wallet file. Mainnet MUST
# use a rotated key (see MAINNET-CHECKLIST.md).
founder-wallet.json
founder-wallet*.json
# The localnet genesis fixture is NOT a secret (public key + lamports only) —
# it must stay tracked for the deterministic anchor test bootstrap.
!tests/genesis/founder-wallet.json
# Pre-existing: other operator wallets must not be tracked either.
enrg-wallet.json
validator.log
test-ledger/
solana-release/
solana-release-x86_64-unknown-linux-gnu.tar.bz2
keypairs/
enrg-wallet.json
.config/
deploy/
# OTA firmware images published by the orchestrator (ADR-0008)
firmware/updates/
# NXP Plug & Trust middleware for the SE050 tier — vendored third-party code
# (BSD-3-Clause, ~105 files) fetched by scripts/vendor-se050.sh into vendor/,
# not lib/ (PlatformIO compiles all of lib/ for every environment). Our own
# ESP32 host port for it (lib/enrg_se050_port) IS committed.
firmware/esp32_proof_sender/vendor/se05x/
# Legacy firmware v1/v2 (archived, NEVER track — D-1, ADR-0001/0007) and the
# pre-SE050 identity helper that stored the key in NVS in plaintext (H-4).
# Only the quarantine note is kept in the repository.
firmware/legacy/*
!firmware/legacy/README.md
# Demo recordings (heavy, generated locally). The script, the captions and the
# narration text ARE tracked — see demo/ and demo/pitch-video/.
demo/*.mp4
demo/voice/

# Foundry build output (Solidity): artefacts and the compiler cache — never
# tracked, `forge build` regenerates them.
onchain/out/
onchain/cache/
